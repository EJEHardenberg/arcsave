{
	"manifest_version" : 2,
	"name" : "*arc, one click archiving",
	"description" : "allows a simple button press to archive webpages to archive.today, if the page is from twitter, tweetsave is used as well",
	"version" : "1.1",
	"permissions" : [
		"http://*/*",
		"https://*/*",
		"tabs"
	],
	"browser_action" : {
		"default_icon" : "icon.png",
		"default_popup" : "popup.html"
	}
}
